# SR P3 MCP Server Configuration
# No API keys required for Sveriges Radio Open API
# This file is for future configuration expansion

# Optional: Custom rate limit (requests per minute)
# RATE_LIMIT_RPM=10

# Optional: API timeout in milliseconds
# API_TIMEOUT_MS=10000
